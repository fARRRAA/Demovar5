<Window x:Class="Demovar5.WindowDialogs.SelectProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Demovar5.WindowDialogs"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="Выбор товара"
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterScreen"
        Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="Выберите товар" 
                   FontFamily="Calibri"
                   FontSize="24" 
                   FontWeight="Bold"
                   Foreground="#0000FF"
                   Margin="0,0,0,20"/>

        <!-- Поиск -->
        <hc:SearchBar Grid.Row="1" 
                      x:Name="SearchBar"
                      Height="40" 
                      FontSize="16"
                      FontFamily="Calibri"
                      hc:InfoElement.Placeholder="Поиск товара..."
                      Style="{StaticResource SearchBarPlus}"
                      TextChanged="SearchBar_TextChanged"
                      Margin="0,0,0,15"/>

        <!-- Список товаров -->
        <ListView Grid.Row="2" 
                  x:Name="ProductsListView"
                  SelectionMode="Single"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  SelectionChanged="ProductsListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Артикул" Width="100" DisplayMemberBinding="{Binding ArticleNumber}"/>
                    <GridViewColumn Header="Наименование" Width="350" DisplayMemberBinding="{Binding ProductName}"/>
                    <GridViewColumn Header="Цена" Width="100" DisplayMemberBinding="{Binding Price}"/>
                    <GridViewColumn Header="Скидка %" Width="80" DisplayMemberBinding="{Binding Discount}"/>
                    <GridViewColumn Header="На складе" Width="90" DisplayMemberBinding="{Binding QuantityInStock}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Количество -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" 
                       Text="Количество:" 
                       FontFamily="Calibri"
                       FontSize="16" 
                       VerticalAlignment="Center"
                       Margin="0,0,15,0"/>

            <hc:NumericUpDown Grid.Column="1"
                              x:Name="QuantityNumeric"
                              Value="1"
                              Minimum="1"
                              Maximum="999"
                              Height="40" 
                              FontSize="16"/>

            <TextBlock Grid.Column="2" 
                       Text="{Binding SelectedProductInfo}" 
                       FontFamily="Calibri"
                       FontSize="14" 
                       Foreground="#666666"
                       VerticalAlignment="Center"
                       Margin="20,0,0,0"
                       TextWrapping="Wrap"/>
        </Grid>

        <!-- Кнопки действий -->
        <Grid Grid.Row="4" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" 
                    Content="Отмена" 
                    Height="45"
                    FontSize="16"
                    Margin="0,0,5,0"
                    Style="{StaticResource ButtonDefault}"
                    Click="Cancel_Click"/>

            <Button Grid.Column="1" 
                    Content="Добавить" 
                    Height="45"
                    FontSize="16"
                    Margin="5,0,0,0"
                    Style="{StaticResource ButtonSuccess}"
                    Click="Add_Click"
                    IsEnabled="{Binding IsProductSelected}"/>
        </Grid>

        <!-- Growl для уведомлений -->
        <ScrollViewer VerticalScrollBarVisibility="Hidden" 
                      Grid.Row="0"
                      HorizontalAlignment="Right">
            <StackPanel hc:Growl.GrowlParent="True" 
                        VerticalAlignment="Top" 
                        Margin="0,10,10,10"/>
        </ScrollViewer>
    </Grid>
</Window>
